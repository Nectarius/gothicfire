apiVersion: v1
kind: ConfigMap
metadata:
  name: gothicfire-env
  namespace: gothicfire
data:
  # Application runs HTTP internally (Behind Traefik Proxy); PROD mode applies secure cookies and correct redirects
  APP_MODE: "PROD"
  BEHIND_REVERSE_PROXY: "true"
  MONGODB_DB: "gothicfire"

  # OAuth redirect URIs (routed through Traefik with TLS termination)
  GOOGLE_REDIRECT_URI: "https://gothiccastles.com/auth/google/callback?provider=google"
  TWITTER_CALLBACK_URL: "https://gothiccastles.com/auth/twitter/callback"

  # OAuth endpoints
  GOOGLE_AUTH_URI: "https://accounts.google.com/o/oauth2/auth"
  GOOGLE_TOKEN_URI: "https://oauth2.googleapis.com/token"
  TWITTER_AUTH_URI: "https://twitter.com/i/oauth2/authorize"
  TWITTER_TOKEN_URI: "https://api.twitter.com/2/oauth2/token"
