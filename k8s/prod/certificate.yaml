apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kornelian-com-tls
  namespace: gothicfire
spec:
  secretName: gothiccastles-com-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - gothiccastles.com
