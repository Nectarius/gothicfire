apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kornelian-com-tls
  namespace: gothicfire
spec:
  secretName: kornelian-com-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - kornelian.com
